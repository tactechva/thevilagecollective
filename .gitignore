node_modules
.next
out
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel
.env*
test-results


# build verification into its own dir, so it cannot clobber a live dev server
.next-*

# screenshots from scripts/shoot.mjs, and one-off diagnostics at the root
.shots
/*.mjs
